# Optional — leave empty to use local storage only (still free, no signup).
# Create a project at https://supabase.com (free tier), then paste URL + anon key.
# Enable Authentication → Providers → Anonymous users.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
